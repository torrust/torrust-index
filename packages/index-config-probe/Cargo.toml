[package]
name = "torrust-index-config-probe"

authors.workspace = true
description = "Resolves the Torrust Index configuration and emits the container-relevant subset as JSON"
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
rust-version.workspace = true
version = "0.1.0"

[dependencies]
torrust-index-cli-common = { version = "0.1.0", path = "../index-cli-common" }
torrust-index-config = { version = "0.1.0", path = "../index-config" }

clap = { version = "4", features = ["derive"] }
percent-encoding = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
url = "2"

[lints]
workspace = true
